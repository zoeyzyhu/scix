# Beginner Start Here

The main beginner guide now lives at [README.md](../README.md).

Open that file first. It explains:

- how to create an empty folder
- how to run `python3 -m pip install --user scix`
- how to run `python3 -m scix up`
- what to do if `scix` says "command not found"
- how to activate `xenv`
- how to log into Codex and Claude
- how to use VS Code
